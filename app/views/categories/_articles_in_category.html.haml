%table
  - articles.each do |a|
    %tr
      %td
        %strong
          = a.title
      %td
        = (truncate(strip_tags(a.content), length:200))
      %td
        = link_to 'Show', article_path(a.id)
